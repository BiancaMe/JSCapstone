import getComments from './GETcomments';

const path = '';

const displayComments = (comments) => {

};
/*
document.querySelector('#btn-comments').addEventListener('clicl', async () => {
  const comments = await getComments(path);
  displayComments(comments);
});*/

